# pnpm Configuration (Configuración de pnpm)
# Required for Nuxt 3 compatibility — hoists packages so Nuxt can resolve them
# (Necesario para compatibilidad con Nuxt 3 — eleva paquetes para que Nuxt pueda resolverlos)
shamefully-hoist=true
