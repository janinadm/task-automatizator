# ============================================================================
# Environment Variables Template (Plantilla de Variables de Entorno)
# ============================================================================
# INSTRUCTIONS (INSTRUCCIONES):
# 1. Copy this file to `.env`: cp .env.example .env
#    (Copiar este archivo a `.env`)
# 2. Fill in your actual values below
#    (Rellenar con tus valores reales abajo)
# 3. NEVER commit the `.env` file to Git — it contains secrets!
#    (¡NUNCA subir el archivo `.env` a Git — contiene secretos!)
# ============================================================================

# === Supabase Configuration (Configuración de Supabase) ===
# Find these in: Supabase Dashboard → Settings → API
# (Encontrar en: Panel de Supabase → Settings → API)

# Your Supabase project URL (Tu URL del proyecto Supabase)
SUPABASE_URL=https://your-project-id.supabase.co

# Public anonymous key — safe to expose to the browser
# (Clave anónima pública — seguro exponer al navegador)
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Service role key — NEVER expose to the client/browser!
# Used server-side for admin operations that bypass RLS
# (Clave de rol de servicio — ¡NUNCA exponer al cliente/navegador!
#  Usado en el servidor para operaciones admin que saltan RLS)
NUXT_SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# === Database Connection (Conexión a Base de Datos) ===
# Find these in: Supabase Dashboard → Settings → Database
# (Encontrar en: Panel de Supabase → Settings → Database)

# Connection pooler URL — used by Prisma Client at runtime
# Use the "Session mode" pooler URL with ?pgbouncer=true
# (URL del pooler de conexiones — usado por Prisma Client en runtime)
DATABASE_URL=postgresql://postgres.xxxx:password@aws-0-region.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1

# Direct connection URL — used ONLY for Prisma migrations
# (URL de conexión directa — usada SOLO para migraciones de Prisma)
DIRECT_URL=postgresql://postgres.xxxx:password@aws-0-region.supabase.com:5432/postgres

# === Google Gemini AI (IA Google Gemini) ===
# Get your API key from: https://aistudio.google.com/apikey
# (Obtener tu API key de: https://aistudio.google.com/apikey)
NUXT_GEMINI_API_KEY=AIzaSy...

# === Vercel Cron Secret (Secreto para Cron Jobs de Vercel) ===
# A random string to validate that cron requests come from Vercel
# Generate with: openssl rand -hex 32
# (Un string aleatorio para validar que las peticiones cron vienen de Vercel)
NUXT_CRON_SECRET=your-random-secret-here

# === Development Settings (Configuración de desarrollo) ===
NODE_ENV=development
